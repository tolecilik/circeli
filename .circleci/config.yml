version: 2.1

jobs:
  run-sshx:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Jalankan SSHX
          command: |
            echo "Menjalankan SSHX.io..."
            curl -sSf https://sshx.io/get | sh -s run

workflows:
  version: 2
  run-it:
    jobs:
      - run-sshx
